
const init = {
   username : ''
}

export default ( state = init, action ) => {
   switch (action.type) {
      case 'LOGIN':
         

      case 'LOGOUT':
         
   
      default:
         return state
   }
}